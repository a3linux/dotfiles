# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# Add $HOME/bin to PATH
mkdir -p $HOME/bin

# User specific environment
# PATH
if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]
then
    PATH="$HOME/.local/bin:$HOME/bin:$PATH"
fi
if [[ -d $HOME/.yarn/bin ]]; then
    PATH=$PATH:$HOME/.yarn/bin
fi
export PATH
export TERM=xterm-256color
export VISUAL=vim
export EDITOR="$VISUAL"
set -o vi

# FZF CMD
FZF_CMD=`which fzf`
RG_CMD=`which rg`
if [ ! -z ${FZF_CMD} ]; then
    if [ -f ~/.fzf.bash ]; then
        . ~/.fzf.bash
    fi
    if [ ! -z ${RG_CMD} ]; then
        export FZF_DEFAULT_COMMAND="rg --files --no-ignore-vcs --hidden"
    fi
fi

# Init Git environment for CLI prompt
if [ -f $HOME/.git_env.sh ]; then
    . $HOME/.git_env.sh
fi
# Load customized aliases
if [ -f $HOME/.bash_aliases ]; then
    . $HOME/.bash_aliases
fi

# vim: ts=4 sw=4 filetype=sh

# Add RVM to PATH for scripting. Make sure this is the last PATH variable change.
export PATH="$PATH:$HOME/.rvm/bin"

# tabtab source for serverless package
# uninstall by removing these lines or running `tabtab uninstall serverless`
[ -f /home/allen/.config/yarn/global/node_modules/tabtab/.completions/serverless.bash ] && . /home/allen/.config/yarn/global/node_modules/tabtab/.completions/serverless.bash
# tabtab source for sls package
# uninstall by removing these lines or running `tabtab uninstall sls`
[ -f /home/allen/.config/yarn/global/node_modules/tabtab/.completions/sls.bash ] && . /home/allen/.config/yarn/global/node_modules/tabtab/.completions/sls.bash
# tabtab source for slss package
# uninstall by removing these lines or running `tabtab uninstall slss`
[ -f /home/allen/.config/yarn/global/node_modules/tabtab/.completions/slss.bash ] && . /home/allen/.config/yarn/global/node_modules/tabtab/.completions/slss.bash